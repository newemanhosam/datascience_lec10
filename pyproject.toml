[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project] 
name = "customer_churn_demo"
version = "0.1.0"
description = "A demo project for customer churn prediction"

[tool.setuptools.packages.find]
where = ["."]
